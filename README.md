# Conquering Responsive Layouts with TailwindCSS!


### How to run project:
-> `npm install` in the terminal to install all the project dependencies

-> `npm run dev` in the terminal to run the project via vite
